{
  "name": "rickkwang-github-io-workspace",
  "private": true,
  "scripts": {
    "bootstrap": "npm --prefix portfolio ci",
    "dev": "npm --prefix portfolio run dev",
    "build": "npm --prefix portfolio run build -- --base=/",
    "preview": "npm --prefix portfolio run preview",
    "publish:local": "npm run build && rm -rf assets && cp -R portfolio/dist/* . && touch .nojekyll"
  }
}
